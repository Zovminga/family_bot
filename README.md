# Family Bot - Telegram бот для учета семейных расходов 💰

Telegram бот для ведения учета семейных расходов с интеграцией Google Sheets.

## ✨ Возможности

- ✅ Добавление трат по категориям с эмодзи
- ✅ Выбор валюты (₽, дин, €, ¥)
- ✅ Автоматическое определение пользователя по Telegram
- ✅ Добавление комментариев
- ✅ Статистика по месяцам и категориям
- ✅ **Произвольный период** для статистики (выбор любых дат)
- ✅ **Конвертация валют** в статистике (приведение всех трат к одной валюте)
- ✅ **Последние 3 записи** - быстрый просмотр недавних трат
- ✅ Группировка статистики по категориям с процентами
- ✅ **Кнопка "🏠 К началу"** для сброса процесса в любой момент
- ✅ Интеграция с Google Sheets
- ✅ Динамическая загрузка категорий из Google Sheets

## 🎮 Команды бота

- `/start` - запуск бота
- `/categories` - показать текущие категории
- `/reloadcats` - перезагрузить категории из Google Sheets
- `/test_connection` - протестировать подключение к Google Sheets
- `/whoami` - показать информацию о профиле
- `/register ИМЯ` - зарегистрировать пользователя
- `/cancel` или `/stop` - отменить текущее действие

## 🚀 Быстрый старт

### 1. Переменные окружения

Создайте файл `.env` или установите переменные окружения:

```bash
BOT_TOKEN=your_telegram_bot_token
GOOGLE_CREDS_PATH=/path/to/your/credentials.json
SHEET_NAME=your_google_sheet_name
RENDER_EXTERNAL_URL=https://your-app.onrender.com  # для деплоя на Render
```

### 2. Google Sheets настройка

#### Структура листа "Config"

В вашем Google Sheets должен быть лист с названием **"Config"**. В этом листе:

- **A1**: Заголовок (например, "Категории" или "Categories")
- **A2, A3, A4...**: Названия категорий

Пример содержимого столбца A:
```
A1: Категории
A2: Продукты
A3: Транспорт
A4: Развлечения
A5: Рестораны
A6: Покупки
A7: Здоровье
A8: Household
A9: Другое
```

#### Структура листа "Data"

Создайте лист **"Data"** для хранения трат со следующими столбцами:
- Дата (дд.мм.гггг)
- Месяц (гггг-мм)
- Категория
- Сумма
- Валюта
- Кто внес
- Комментарий

### 3. Google Cloud настройка

1. Создайте проект в Google Cloud Console
2. Включите Google Sheets API
3. Создайте сервисный аккаунт
4. Скачайте JSON файл с credentials
5. Предоставьте доступ к таблице для email сервисного аккаунта

### 4. Установка и запуск

#### Локальная разработка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd family_bot

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте переменные окружения
export BOT_TOKEN=your_token
export GOOGLE_CREDS_PATH=/path/to/credentials.json
export SHEET_NAME=your_sheet_name

# Запустите бота
python bot.py
```

#### Тестирование подключения

```bash
python test_google_sheets.py
```

#### Деплой на Render

1. Подключите репозиторий к Render
2. Настройте переменные окружения в Render Dashboard
3. Укажите команду запуска: `python bot.py`
4. Укажите порт: `8443`

## 📖 Подробная документация

### Настройка автоматического определения пользователей

Бот автоматически определяет, кто внес трату, по Telegram аккаунту - никаких лишних выборов!

#### Команды для работы с пользователями

- **`/whoami`** - Информация о профиле
  - Показывает ваш Telegram ID и статус регистрации.

- **`/register ИМЯ`** - Регистрация пользователя
  - Регистрирует вас в системе с указанным именем.
  - Примеры: `/register Лиза`, `/register Азат`

#### Настройка в коде

1. Получите Telegram ID пользователей (попросите каждого выполнить `/whoami`)
2. В файле `bot.py` найдите секцию `TELEGRAM_USERS` и добавьте пользователей:

```python
TELEGRAM_USERS = {
    123456789: "Лиза",      # Замените на реальный ID
    987654321: "Азат",      # Замените на реальный ID
}
```

3. Перезапустите бота

### Статистика

#### Группировка по категориям

При выборе "Показать статистику" доступна группировка по категориям:
- Детальная статистика с процентами
- Сортировка категорий по убыванию суммы
- Разбивка по валютам для каждой категории

#### Произвольный период

Вместо выбора календарного месяца можно выбрать произвольный период:
1. Выберите "📅 Выбрать период"
2. Введите начальную дату (дд.мм.гггг)
3. Введите конечную дату (дд.мм.гггг)
4. Выберите валюту для конвертации (или оставьте как есть)
5. Выберите нужна ли группировка по категориям

#### Конвертация валют

При выборе произвольного периода доступна конвертация всех трат в одну валюту:
- Используется текущий курс из API exchangerate-api.com
- Поддерживаемые валюты: ₽ (RUB), дин (RSD), € (EUR), ¥ (JPY)
- Можно оставить как есть с группировкой по валютам

#### Последние записи

Кнопка "📜 Последние 3 записи" показывает последние 3 записи трат из Google Sheets с полной информацией:
- Дата
- Категория
- Сумма и валюта
- Кто внес
- Комментарий (если есть)

### Кнопка "К началу"

В любой момент процесса добавления траты или просмотра статистики можно нажать кнопку "🏠 К началу" для возврата в главное меню. Это аналог команды `/start`.

## 🔧 Устранение неполадок

### Категории не загружаются
1. Проверьте название листа "Config" (должно быть точно "Config")
2. Убедитесь, что в столбце A есть данные
3. Используйте команду `/test_connection`
4. Используйте команду `/reloadcats` для принудительного обновления

### Ошибка подключения к Google Sheets
1. Проверьте путь к файлу credentials в переменной `GOOGLE_CREDS_PATH`
2. Убедитесь, что сервисный аккаунт имеет доступ к таблице
3. Проверьте название таблицы в переменной `SHEET_NAME`
4. Проверьте, что Google Sheets API включен в Google Cloud Console

### Курсы валют не работают
1. Проверьте интернет-соединение
2. API exchangerate-api.com работает без ключа для базовых валют
3. Убедитесь, что валюта поддерживается (₽, дин, €, ¥)

### Бот не отвечает
1. Проверьте токен бота в переменной `BOT_TOKEN`
2. Убедитесь, что бот запущен
3. Проверьте логи в консоли
4. Для webhook режима проверьте переменную `RENDER_EXTERNAL_URL`

### Пользователь не определяется
1. Проверьте правильность Telegram ID в `TELEGRAM_USERS`
2. Убедитесь, что ID указан как число, а не строка
3. Попросите пользователя выполнить `/whoami` для получения ID
4. Перезапустите бота после изменений

## 💡 Рекомендации

1. **Регистрируйте всех пользователей** для удобства
2. **Используйте короткие имена** (2-3 символа) в `TELEGRAM_USERS`
3. **Синхронизируйте имена** в `TELEGRAM_USERS` и `SPENDERS`
4. **Регулярно обновляйте категории** через Google Sheets
5. **Используйте кнопку "К началу"** если ошиблись при вводе

## 🔄 Автоматическое обновление категорий

- Категории загружаются автоматически при запуске бота
- Используйте `/reloadcats` для принудительного обновления
- При ошибке загрузки используются базовые категории
- Категории валидируются (удаляются дубликаты, очищаются пробелы)

## 📊 Примеры использования

### Добавление траты

1. Нажмите "💰 Добавить трату"
2. Выберите категорию
3. Введите сумму
4. Выберите валюту
5. Добавьте комментарий (или пропустите)
6. Выберите дату (сегодня или укажите)

### Просмотр статистики за месяц

1. Нажмите "📊 Показать статистику"
2. Выберите категорию (или "Все")
3. Выберите месяц
4. Выберите нужна ли группировка по категориям

### Просмотр статистики за произвольный период

1. Нажмите "📊 Показать статистику"
2. Выберите категорию (или "Все")
3. Нажмите "📅 Выбрать период"
4. Введите начальную дату
5. Введите конечную дату
6. Выберите валюту для конвертации (или оставьте как есть)
7. Выберите нужна ли группировка

## 🛠️ Разработка

### Добавление новых функций

1. Создайте новую функцию-обработчик
2. Добавьте обработчик в ConversationHandler
3. Обновите состояния диалога при необходимости

### Структура кода

- `bot.py` - основной файл бота
- `test_google_sheets.py` - скрипт для тестирования
- `requirements.txt` - зависимости
- `README.md` - полная документация (этот файл)

### Состояния диалога

Бот использует ConversationHandler со следующими состояниями:
- `CHOOSE_ACTION` - выбор действия
- `CHOOSE_CAT` - выбор категории
- `TYPING_AMT` - ввод суммы
- `CHOOSE_CUR` - выбор валюты
- `TYPING_CMNT` - ввод комментария
- `CHOOSE_DT` - выбор даты
- `TYPING_DT` - ввод даты
- `STAT_CAT` - выбор категории для статистики
- `STAT_MON` - выбор месяца
- `STAT_DATE_FROM` - ввод начальной даты
- `STAT_DATE_TO` - ввод конечной даты
- `STAT_CURRENCY_CONVERT` - выбор валюты для конвертации
- `STAT_GROUP` - выбор группировки

## 📝 Лицензия

MIT License
